import { CollapsibleNavDirective } from './collapsible-nav.directive';

describe('CollapsibleNavDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapsibleNavDirective();
    expect(directive).toBeTruthy();
  });
});
