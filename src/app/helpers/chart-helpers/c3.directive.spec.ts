import { C3Directive } from './c3.directive';

describe('C3Directive', () => {
  it('should create an instance', () => {
    const directive = new C3Directive();
    expect(directive).toBeTruthy();
  });
});
