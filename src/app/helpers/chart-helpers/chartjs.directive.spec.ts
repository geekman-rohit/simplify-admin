import { ChartjsDirective } from './chartjs.directive';

describe('ChartjsDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartjsDirective();
    expect(directive).toBeTruthy();
  });
});
