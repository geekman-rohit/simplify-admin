import { PlotlyDirective } from './plotly.directive';

describe('PlotlyDirective', () => {
  it('should create an instance', () => {
    const directive = new PlotlyDirective();
    expect(directive).toBeTruthy();
  });
});
