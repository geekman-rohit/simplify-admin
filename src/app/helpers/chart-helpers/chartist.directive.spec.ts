import { ChartistDirective } from './chartist.directive';

describe('ChartistDirective', () => {
  it('should create an instance', () => {
    const directive = new ChartistDirective();
    expect(directive).toBeTruthy();
  });
});
