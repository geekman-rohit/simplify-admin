import { FullCalendarEventDirective } from './full-calendar-event.directive';

describe('FullCalendarEventDirective', () => {
  it('should create an instance', () => {
    const directive = new FullCalendarEventDirective();
    expect(directive).toBeTruthy();
  });
});
